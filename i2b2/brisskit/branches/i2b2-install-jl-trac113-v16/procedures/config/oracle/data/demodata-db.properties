# Database setup parameters 
db.type=${db.type}
db.username=${demods.username}
db.password=${demods.password}
# Jeff Lusted. I think the following parameter may be redundant.
db.server=${database.server}
db.driver=${db.driver}
db.url=${jdbc.url}
db.project=demo